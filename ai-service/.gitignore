# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
.eggs/

# Virtual environment
venv/
env/
.venv/
ENV/

# Environment variables — NEVER commit
.env
.env.local
*.env

# Logs
*.log
logs/

# HuggingFace / Whisper model weight cache (large files) — note we
# deliberately do NOT ignore the `models/` folder, which holds the
# Pydantic request/response schemas. Targeting the binary weight
# extensions and the explicit `.cache/` paths instead.
.cache/
hf_cache/
whisper_cache/
*.bin
*.pt
*.pth
*.onnx
*.safetensors

# OS
.DS_Store
Thumbs.db
