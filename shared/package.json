{
  "name": "@dreamsignal/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./contracts.js",
  "description": "Shared API contracts (Zod schemas) consumed by both the backend (via dynamic import) and the frontend (via Vite @shared alias)."
}
